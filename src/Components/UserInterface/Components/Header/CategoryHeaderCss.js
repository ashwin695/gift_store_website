import { makeStyles } from "@mui/styles";

const categoryHeaderStyles = makeStyles({
    center:{
        display:'flex',
        justifyContent:'center',
        alignItems:'center',
        flexDirection:'column'
    },
    button:{
        color:'#c6186d',
    }
})

export default categoryHeaderStyles